---
phase: 14-sanitization-classifier
plan: "03"
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/ui/components/CloudRequestPreview.tsx
  - src/ui/layout/Shell.tsx
  - src/ui/layout/layout.css
  - src/ui/signals/store.ts
autonomous: false

requirements: [SNTZ-03]

must_haves:
  truths:
    - "Pre-send modal shows pseudonymized text (what cloud will see) instead of raw text"
    - "Expandable entity mapping table shows pseudonym-to-real-text mappings (collapsed by default)"
    - "User can click individual entities in the mapping table to toggle restore (un-redact)"
    - "Restore preferences are remembered across requests via entity registry"
    - "Cloud responses are de-pseudonymized before showing to user"
  artifacts:
    - path: "src/ui/components/CloudRequestPreview.tsx"
      provides: "Extended pre-send modal with entity map table and restore toggles"
      min_lines: 80
    - path: "src/ui/layout/layout.css"
      provides: "CSS for entity mapping table, restore toggles, pseudonym highlights"
  key_links:
    - from: "src/ui/components/CloudRequestPreview.tsx"
      to: "src/ai/sanitization/entity-registry.ts"
      via: "setRestorePreference calls on toggle"
      pattern: "setRestorePreference"
    - from: "src/ui/layout/Shell.tsx"
      to: "src/ai/sanitization/sanitizer.ts"
      via: "sanitizeText call before showing modal"
      pattern: "sanitizeText"
---

<objective>
Extend the pre-send approval modal to display pseudonymized text with an expandable entity mapping table and per-entity restore toggles. Wire de-pseudonymization of cloud responses in the Shell/store layer.

Purpose: Users see exactly what was redacted before approving cloud dispatch (SNTZ-03), with the ability to un-redact specific entities. The round-trip is seamless: cloud responses containing pseudonym tags are automatically de-pseudonymized before display.

Output: Enhanced CloudRequestPreview with entity awareness, restore toggle persistence, and response de-pseudonymization.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sanitization-classifier/14-CONTEXT.md
@.planning/phases/14-sanitization-classifier/14-RESEARCH.md
@.planning/phases/14-sanitization-classifier/14-02-SUMMARY.md

# Source files to read before modifying
@src/ui/components/CloudRequestPreview.tsx
@src/ui/layout/Shell.tsx
@src/ui/layout/layout.css
@src/ui/signals/store.ts

<interfaces>
<!-- From Plan 02 — types and functions this plan consumes -->

From src/ai/sanitization/types.ts:
```typescript
export type SanitizedPrompt = string & { readonly [__sanitized]: true };
export type EntityCategory = 'PERSON' | 'LOCATION' | 'FINANCIAL' | 'CONTACT' | 'CREDENTIAL';
export interface DetectedEntity {
  text: string;
  category: EntityCategory;
  start: number;
  end: number;
  source: 'ner' | 'regex' | 'both';
  confidence: number;
}
export interface SanitizedResult {
  prompt: SanitizedPrompt;
  entities: DetectedEntity[];
  entityMap: Map<string, string>;    // pseudonym -> real text
  reverseMap: Map<string, string>;   // real text -> pseudonym
}
```

From src/ai/sanitization/entity-registry.ts:
```typescript
export function setRestorePreference(realText: string, category: EntityCategory, restore: boolean): Promise<void>;
export function getRestorePreference(realText: string, category: EntityCategory): Promise<boolean>;
```

From src/ai/sanitization/sanitizer.ts:
```typescript
export function sanitizeText(text: string): Promise<SanitizedResult>;
export function dePseudonymize(response: string, entityMap: Map<string, string>): string;
```

From src/ai/key-vault.ts:
```typescript
export interface CloudRequestLogEntry {
  id: string;
  timestamp: number;
  sanitizedPrompt: SanitizedPrompt;
  provider: string;
  model: string;
  baseURL?: string;
  status: 'pending' | 'approved' | 'cancelled' | 'sent' | 'error';
  response?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CloudRequestPreview entity map UI, restore toggles, and CSS</name>
  <files>
    src/ui/components/CloudRequestPreview.tsx
    src/ui/layout/Shell.tsx
    src/ui/layout/layout.css
    src/ui/signals/store.ts
  </files>
  <action>
Extend the pre-send modal and wiring to show pseudonymized text with entity controls.

**src/ui/components/CloudRequestPreview.tsx** — Enhanced pre-send modal:
- Add new props to CloudRequestPreviewProps:
  - `entities?: DetectedEntity[]` — detected entities (empty if no sanitization ran)
  - `entityMap?: Map<string, string>` — pseudonym -> real text mapping
- Import: `createSignal, For, Show` from solid-js; `DetectedEntity, EntityCategory` from sanitization/types; `setRestorePreference` from sanitization/entity-registry
- Add a local signal `const [mapExpanded, setMapExpanded] = createSignal(false)` for the collapsible mapping table
- Add a local signal `const [restoredEntities, setRestoredEntities] = createSignal<Set<string>>(new Set())` tracking which entities the user has toggled to restore in this modal session
- CRITICAL: Never destructure props — use props.entities, props.entityMap, etc. (SolidJS reactivity rule)
- After the "Data being sent:" section and the sanitized prompt display, add:
  1. Entity count badge: "N entities redacted" (or "No entities detected" if empty) shown above the prompt
  2. Expandable mapping table (collapsed by default):
     - Toggle button: "Show entity map" / "Hide entity map" with expand/collapse arrow
     - Table with columns: Pseudonym | Real Value | Category | Action
     - Each row shows: `<Person 12>` | `John Smith` | PERSON | [Restore] toggle button
     - Restore toggle: clicking calls `setRestorePreference(realText, category, true)` and adds to restoredEntities signal
     - Already-restored entities show a different style (muted, with "Restored" label)
  3. If any entities are restored, show a note: "N entities will be sent un-redacted to the cloud"
- The prompt display (`<pre>` block) should show the PSEUDONYMIZED text (props.entry.sanitizedPrompt) — this is what the cloud will see
- Style pseudonym tags in the prompt display with inline highlighting (use a span with class for each `<Category N>` pattern via a simple regex-replace in a computed)

**src/ui/layout/Shell.tsx** — Wire sanitization result into modal:
- Read Shell.tsx to understand how pendingCloudRequest triggers CloudRequestPreview
- The sanitization already happens in the cloud adapter (Plan 02). The SanitizedResult (entities, entityMap) needs to flow to the modal.
- Extend the store's pendingCloudRequest state to include: `entities?: DetectedEntity[]`, `entityMap?: Map<string, string>`
- The cloud adapter's pre-send approval handler (set via setPreSendApprovalHandler) currently receives the CloudRequestLogEntry. Extend this to also pass entities and entityMap from the SanitizedResult.
- In Shell.tsx where CloudRequestPreview is rendered, pass the entities and entityMap props from the pending request state.

**src/ui/signals/store.ts** — Extend pending request state:
- Add `entities` and `entityMap` fields to the pendingCloudRequest state shape
- When the pre-send approval handler is called (from cloud adapter), store the entities and entityMap alongside the log entry

**src/ui/layout/layout.css** — Entity map styles:
- `.entity-map-toggle` — button style for expand/collapse, subtle with arrow indicator
- `.entity-map-table` — table with compact rows, alternating background for readability
- `.entity-map-pseudonym` — monospace, colored per category (e.g., PERSON=blue, LOCATION=green, FINANCIAL=orange, CONTACT=purple, CREDENTIAL=red)
- `.entity-map-real` — normal text, slightly muted
- `.entity-map-category` — small badge style matching the color scheme
- `.entity-map-restore-btn` — toggle button, outline style, changes to filled when entity is restored
- `.entity-map-restored` — muted row style for restored entities
- `.entity-count-badge` — small pill badge above the prompt showing "N entities redacted"
- `.pseudonym-highlight` — inline highlight for pseudonym tags within the prompt text (background color per category)
- `.entity-restore-warning` — subtle warning text for "N entities will be sent un-redacted"
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -v "node_modules" | grep -v "VoiceCapture" | grep -v "vite.config" | head -20</automated>
  </verify>
  <done>
    - CloudRequestPreview shows pseudonymized text as the prompt being sent
    - Entity count badge displays number of redacted entities
    - Expandable mapping table (collapsed by default) shows pseudonym-to-real-text entries with categories
    - Per-entity restore toggle calls setRestorePreference and updates local state
    - Pseudonym tags in prompt text are visually highlighted
    - CSS provides distinct colors per entity category
    - TypeScript compiles and pnpm build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sanitization round-trip end-to-end</name>
  <files>src/ui/components/CloudRequestPreview.tsx</files>
  <action>
Human verification of the complete sanitization round-trip. No code changes — verify the pipeline built across Plans 01-03 works end-to-end.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>User confirms sanitization pipeline works: entities detected, pseudonymized in modal, restore toggles functional, de-pseudonymized responses display correctly</done>
  <what-built>
    Complete sanitization pipeline: NER + regex entity detection, pseudonymized pre-send modal with entity mapping table and restore toggles, de-pseudonymized cloud responses. The full round-trip: user initiates cloud request -> entities detected and pseudonymized -> pre-send modal shows pseudonymized text with entity map -> user approves -> cloud receives pseudonymized text -> response de-pseudonymized before display.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` to start the dev server
    2. Enable cloud AI with any provider (Anthropic, OpenAI, etc.) and set an API key
    3. Create an atom with PII content, e.g.: "Meeting with John Smith at New York office, call him at john@example.com. Budget: $15,000"
    4. Trigger an AI action on that atom (triage, review, etc.) that would go to cloud
    5. Verify the pre-send modal appears showing:
       - Pseudonymized prompt text (e.g., "Meeting with <Person 1> at <Location 1> office...")
       - Entity count badge ("N entities redacted")
       - "Show entity map" toggle button
    6. Click "Show entity map" — verify the table shows:
       - `<Person 1>` -> `John Smith` (PERSON)
       - `<Location 1>` -> `New York` (LOCATION)
       - `<Contact 1>` -> `john@example.com` (CONTACT)
       - `<Financial 1>` -> `$15,000` (FINANCIAL)
    7. Click "Restore" on one entity — verify it updates visually
    8. Approve the request — verify the cloud receives pseudonymized text
    9. Verify the AI response is de-pseudonymized (real names appear in the suggestion)
    10. Repeat with a clean atom (no PII) — verify "No entities detected" and no entity map
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` passes (excluding pre-existing errors)
2. `pnpm build` succeeds
3. Pre-send modal shows pseudonymized text with entity mapping table
4. Entity restore toggles update preferences in IndexedDB
5. Cloud responses show real values (de-pseudonymized) in AI suggestions
</verification>

<success_criteria>
- Pre-send modal displays pseudonymized text and entity mapping table with restore toggles
- User can see exactly what was redacted before approving cloud dispatch
- Restore preferences persist across sessions via entity registry
- Cloud responses are seamlessly de-pseudonymized before display
- Full round-trip works: detect -> pseudonymize -> approve -> send -> receive -> de-pseudonymize
</success_criteria>

<output>
After completion, create `.planning/phases/14-sanitization-classifier/14-03-SUMMARY.md`
</output>
