---
phase: 24-unified-enrichment-wizard
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/components/ThreeRingIndicator.tsx]
autonomous: true
gap_closure: true
requirements: [ENRICH-08]

must_haves:
  truths:
    - "ThreeRingIndicator imports getTiersUsed and getModelNames from provenance.ts, not local stubs"
    - "3-Ring indicator correctly lights up rings for real provenance bitmasks using grouped MODEL_IDS"
  artifacts:
    - path: "src/ui/components/ThreeRingIndicator.tsx"
      provides: "3-Ring SVG provenance indicator wired to real provenance module"
      contains: "import.*provenance"
  key_links:
    - from: "src/ui/components/ThreeRingIndicator.tsx"
      to: "src/ai/enrichment/provenance.ts"
      via: "import { getTiersUsed, getModelNames }"
      pattern: "import.*getTiersUsed.*getModelNames.*provenance"
---

<objective>
Replace local stub functions in ThreeRingIndicator.tsx with imports from the real provenance.ts module.

Purpose: The stub bitmask logic (individual bits 0-3) diverges from the real provenance module which groups multiple MODEL_IDS per tier (e.g., t1 = TYPE_ONNX | GTD_ROUTING). Without this fix, the 3-Ring indicator will not correctly light up rings for real provenance data.
Output: ThreeRingIndicator.tsx using real provenance functions, ~30 lines of stub code removed.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-unified-enrichment-wizard/24-VERIFICATION.md

<interfaces>
<!-- Real provenance module exports that ThreeRingIndicator must use -->

From src/ai/enrichment/provenance.ts:
```typescript
export function getTiersUsed(bitmask: number): {
  t1: boolean;   // TYPE_ONNX | GTD_ROUTING
  t2a: boolean;  // DECOMPOSE_ONNX | COMPLETENESS | MISSING_INFO
  t2b: boolean;  // WASM_LLM
  t3: boolean;   // CLOUD_LLM
};

export function getModelNames(bitmask: number): string[];
// Returns human-readable names like 'Type ONNX', 'GTD Routing', etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ThreeRingIndicator stubs with real provenance imports</name>
  <files>src/ui/components/ThreeRingIndicator.tsx</files>
  <action>
In src/ui/components/ThreeRingIndicator.tsx:

1. Add import at top of file (after the JSDoc comment block):
   `import { getTiersUsed, getModelNames } from '../../ai/enrichment/provenance';`

2. Delete lines 16-36 entirely -- the stub comment, the local `getTiersUsed` function (lines 20-27), and the local `getModelNames` function (lines 29-36). These are the ~20 lines of stub code that check individual bits 0-3 instead of using the real grouped MODEL_IDS.

3. Do NOT change anything else in the file. The ThreeRingIndicator component (line 52 onward) already calls `getTiersUsed(props.provenance)` and `getModelNames(props.provenance)` with the correct signatures -- the real provenance.ts exports match the stub signatures exactly (same parameter type, same return type shape).

4. Verify the import path is correct: ThreeRingIndicator is at `src/ui/components/` and provenance is at `src/ai/enrichment/`, so the relative path is `../../ai/enrichment/provenance`.
  </action>
  <verify>
    <automated>cd /c/Users/patri/GSD/BinderOS && npx tsc --noEmit --pretty 2>&1 | grep -E "ThreeRingIndicator|provenance" | head -20; echo "---"; grep -n "import.*provenance" src/ui/components/ThreeRingIndicator.tsx; echo "---"; grep -c "function getTiersUsed" src/ui/components/ThreeRingIndicator.tsx</automated>
  </verify>
  <done>ThreeRingIndicator.tsx imports getTiersUsed and getModelNames from provenance.ts. No local stub functions remain. TypeScript compiles without errors for this file. The grep for local "function getTiersUsed" returns 0 matches.</done>
</task>

</tasks>

<verification>
1. `grep -n "import.*provenance" src/ui/components/ThreeRingIndicator.tsx` shows the import line
2. `grep -c "function getTiersUsed" src/ui/components/ThreeRingIndicator.tsx` returns 0 (no local stubs)
3. `grep -c "function getModelNames" src/ui/components/ThreeRingIndicator.tsx` returns 0 (no local stubs)
4. `npx tsc --noEmit` passes for ThreeRingIndicator.tsx (import resolves correctly)
5. Existing provenance tests still pass: `npx vitest run src/ai/enrichment/provenance.test.ts`
</verification>

<success_criteria>
- ThreeRingIndicator.tsx uses real provenance.ts exports (not stubs)
- No local getTiersUsed/getModelNames function definitions in ThreeRingIndicator.tsx
- TypeScript compilation succeeds
- All 74 existing enrichment tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-unified-enrichment-wizard/24-07-SUMMARY.md`
</output>
