---
phase: 29-entity-consumers-trained-agent-validation
plan: 05
type: execute
wave: 2
depends_on: ["29-04"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TVAL-01, TVAL-02]

must_haves:
  truths:
    - "The adversarial training loop has been executed producing actual output files (experiments/ directory, cycle checkpoint files)"
    - "All personas achieve >= 80% relationship F1 after cycle 1"
    - "All personas achieve >= 90% relationship F1 after 5 cycles"
    - "Investment report generated with ablation results and ranked ONNX recommendations"
  artifacts:
    - path: "scripts/harness/experiments/phase29-validation/experiment-report.md"
      provides: "Cross-persona aggregate results showing F1 benchmarks achieved"
    - path: "scripts/harness/personas/alex-jordan/graphs/cycle_1.json"
      provides: "Cycle checkpoint confirming the loop executed (representative persona)"
    - path: "scripts/harness/experiments/phase29-validation/investment-report.md"
      provides: "Ranked ONNX agent recommendations with impact+complexity matrix"
  key_links:
    - from: "scripts/harness/run-adversarial.ts"
      to: "scripts/harness/experiments/phase29-validation/"
      via: "--experiment phase29-validation flag writes all output here"
      pattern: "phase29-validation"
---

<objective>
Execute the adversarial training loop to produce real benchmark results. The tooling is fully built and verified — this plan runs it.

Purpose: TVAL-01 requires the benchmark to be ACHIEVED, not just the tooling to exist. TVAL-02 requires an actual investment report derived from real ablation data. This plan closes both gaps by running the harness with all 12 personas and 5 cycles.

Output: Actual experiment results (not just tooling), cycle checkpoint files for all personas, cross-persona F1 metrics, investment report with data-backed recommendations.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-entity-consumers-trained-agent-validation/29-CONTEXT.md
@.planning/phases/29-entity-consumers-trained-agent-validation/29-01-SUMMARY.md
@.planning/phases/29-entity-consumers-trained-agent-validation/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight validation and dry-run confirmation</name>
  <files>scripts/harness/run-adversarial.ts</files>
  <action>
Run the dry-run validation to confirm all 12 personas are present and the harness is ready to execute:

```bash
npx tsx scripts/harness/run-adversarial.ts --dry-run --personas all 2>&1
```

Expected output: lists all 12 persona names (alex-jordan, dev-kumar, maria-santos, plus the 9 generated in Plan 01), confirms 5 cycles each, shows estimated API call count.

If the dry-run fails due to missing persona files, generate missing personas first:
```bash
npx tsx scripts/harness/generate-persona.ts --archetype margaret-chen --complexity high
npx tsx scripts/harness/generate-persona.ts --archetype james-okafor --complexity low
# ...etc for each missing archetype
```

Then re-run dry-run until it confirms all 12 personas found. Do NOT proceed to Task 2 until dry-run exits cleanly.

Verify TypeScript compiles before running:
```bash
npx tsc --noEmit 2>&1 | grep -v node_modules | grep -v VoiceCapture | head -10
```
  </action>
  <verify>
    <automated>npx tsx scripts/harness/run-adversarial.ts --dry-run --personas all 2>&1 | grep -i "12\|persona\|cycle"</automated>
  </verify>
  <done>Dry-run confirms 12 personas found, 5 cycles each. No missing synthetic-user.json files. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute full adversarial training run and verify benchmark results</name>
  <files>scripts/harness/experiments/phase29-validation/experiment-report.md, scripts/harness/experiments/phase29-validation/investment-report.md</files>
  <action>
The harness tooling is fully built. This task executes it.

Run the full adversarial training loop:

```bash
ANTHROPIC_API_KEY=<your-key> npx tsx scripts/harness/run-adversarial.ts \
  --personas all \
  --cycles 5 \
  --experiment phase29-validation
```

This will take 30-60 minutes. The harness supports resume via --resume if interrupted. Cost: ~$20-30 for all 12 personas (Haiku for emulation, Sonnet for gap analysis).

After completion, check:
1. `ls scripts/harness/personas/*/graphs/cycle_*.json` — 60 files expected (12 x 5)
2. `cat scripts/harness/experiments/phase29-validation/experiment-report.md` — shows per-persona F1 per cycle
3. `ls scripts/harness/experiments/phase29-validation/investment-report.md` — exists

Benchmarks (TVAL-01):
- Cycle 1 F1 >= 0.80 for all personas
- Cycle 5 F1 >= 0.90 for all personas

If any persona falls below threshold: document in experiment-report.md. Does NOT block v5.0 completion.
  </action>
  <verify>
    <automated>MISSING — requires ANTHROPIC_API_KEY and real API execution (~$20-30). Human must run: ANTHROPIC_API_KEY=key npx tsx scripts/harness/run-adversarial.ts --personas all --cycles 5 --experiment phase29-validation</automated>
  </verify>
  <done>experiments/phase29-validation/experiment-report.md exists showing cross-persona F1 metrics. All personas achieve >= 80% F1 cycle 1 and >= 90% F1 cycle 5. investment-report.md generated with ranked ONNX recommendations. Cycle checkpoint files exist for all personas.</done>
  <what-built>
Full adversarial training harness with 12 diverse personas, 5-cycle gap-targeted loop, enrichment emulation, correction ripple, ablation engine, auto-tune, and investment report generator. All tooling verified working via dry-run.

Benchmark criteria (per TVAL-01):
- >= 80% relationship F1 after cycle 1 (natural corpus)
- >= 90% relationship F1 after 5 cycles (gap-targeted)
  </what-built>
  <how-to-verify>
Run the full adversarial training loop:

```bash
ANTHROPIC_API_KEY=<your-key> npx tsx scripts/harness/run-adversarial.ts \
  --personas all \
  --cycles 5 \
  --experiment phase29-validation
```

After completion, verify:

1. Cycle checkpoints exist: `ls scripts/harness/personas/*/graphs/cycle_*.json` — 60 files
2. Experiment report: `cat scripts/harness/experiments/phase29-validation/experiment-report.md`
   Expected: ALL personas show Cycle 1 F1 >= 0.80 and Cycle 5 F1 >= 0.90
3. Investment report: `ls scripts/harness/experiments/phase29-validation/investment-report.md`

If some personas fall below threshold: check experiment-report.md for patterns. The investment report will recommend targeted fixes. Document findings and proceed — does NOT block v5.0.
  </how-to-verify>
  <resume-signal>Type "approved: [cycle1-min-F1] / [cycle5-min-F1]" with actual metrics, or describe findings if below threshold</resume-signal>
</task>

</tasks>

<verification>
1. `ls scripts/harness/personas/*/graphs/cycle_*.json | wc -l` — should be >= 12 (at minimum cycle_1 for each persona)
2. `ls scripts/harness/experiments/phase29-validation/` — experiment-report.md and investment-report.md present
3. `grep -i "F1\|relation" scripts/harness/experiments/phase29-validation/experiment-report.md | head -20` — shows passing metrics
</verification>

<success_criteria>
- All 12 personas have completed at least 1 full adversarial cycle (cycle checkpoint files exist)
- experiment-report.md exists and contains cross-persona F1 metrics
- Cycle 1 relationship F1 >= 80% for all personas (TVAL-01 cycle 1 benchmark)
- Cycle 5 relationship F1 >= 90% for all personas (TVAL-01 full benchmark)
- investment-report.md exists with ranked ONNX agent recommendations (TVAL-02)
- If benchmarks not fully met: findings documented in experiment-report.md for investigation
</success_criteria>

<output>
After completion, create `.planning/phases/29-entity-consumers-trained-agent-validation/29-05-SUMMARY.md`
</output>
