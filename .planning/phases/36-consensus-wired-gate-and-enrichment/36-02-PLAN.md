---
phase: 36-consensus-wired-gate-and-enrichment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train/70_train_specialist_models.py
  - public/models/specialists/time-pressure-risk.onnx
  - public/models/specialists/dependency-risk.onnx
  - public/models/specialists/staleness-risk.onnx
  - public/models/specialists/energy-context-risk.onnx
autonomous: true
requirements:
  - CONS-01

must_haves:
  truths:
    - "4 specialist ONNX models exist in public/models/specialists/, each under 20KB"
    - "Each model accepts the correct number of float features as input and outputs a 2-class probability array"
    - "Training uses the same ground-truth risk formula as eii-experiment.py"
    - "Feature slices are derived from vectors.json dimension names, not hardcoded indices"
  artifacts:
    - path: "scripts/train/70_train_specialist_models.py"
      provides: "Production specialist training pipeline"
      min_lines: 100
    - path: "public/models/specialists/time-pressure-risk.onnx"
      provides: "TimePressure specialist ONNX model"
    - path: "public/models/specialists/dependency-risk.onnx"
      provides: "Dependency specialist ONNX model"
    - path: "public/models/specialists/staleness-risk.onnx"
      provides: "Staleness specialist ONNX model"
    - path: "public/models/specialists/energy-context-risk.onnx"
      provides: "EnergyContext specialist ONNX model"
  key_links:
    - from: "scripts/train/70_train_specialist_models.py"
      to: "src/config/binder-types/gtd-personal/vectors.json"
      via: "loads vectors.json to derive feature slice indices by dimension name"
      pattern: "vectors\\.json"
    - from: "scripts/train/70_train_specialist_models.py"
      to: "scripts/eii-experiment.py"
      via: "reuses compute_ground_truth_risk formula verbatim"
      pattern: "compute_ground_truth_risk"
---

<objective>
Train 4 specialist MLP risk models on non-overlapping slices of the 84-dim canonical feature vector, each under 20KB, and export as ONNX to public/models/specialists/.

Purpose: Produce the ONNX model artifacts that the consensus worker will load at runtime. The training script loads vectors.json to derive feature slice indices by dimension name — same source of truth as the TypeScript code.
Output: 4 ONNX models + production training script.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-consensus-wired-gate-and-enrichment/36-RESEARCH.md

<interfaces>
<!-- Source of truth for dimension names and feature slices -->

From src/config/binder-types/gtd-personal/vectors.json — AUTHORITATIVE dimension ordering:
- task (27 dims): age_norm, staleness_norm, has_deadline, days_to_deadline_norm, status_open, status_done, status_dropped, has_project, is_waiting_for, ctx_home, ctx_office, ctx_phone, ctx_computer, ctx_errands, ctx_anywhere, energy_low, energy_medium, energy_high, enrichment_depth_norm, has_person_dep, time_pressure_score, prev_staleness_score, prev_energy_fit, entity_reliability, entity_resp_fast, entity_resp_slow, entity_resp_unknown
- person (23 dims): rel_spouse, rel_parent, rel_child, rel_colleague, rel_reports_to, rel_healthcare, rel_friend, rel_org_member, rel_unknown, mention_count_norm, recency_norm, days_since_seen_norm, has_user_correction, confidence_norm, collab_low, collab_medium, collab_high, reliability_score, alias_count_norm, resp_fast, resp_normal, resp_slow, resp_unknown
- calendar (34 dims): start_tod_norm, dow_mon..dow_sun (7), dur_lt30..dur_gt120 (4), energy_low..energy_high (3), has_deadline, days_to_event_norm, time_pressure_score, overrun_risk, slack_before_none..slack_before_long (4), entity_is_high_priority, entity_reliability, mobility_required, is_recurring, prep_none..prep_long (4), has_person_entity, has_org_entity, has_loc_entity

From scripts/eii-experiment.py — ground truth risk formula and training architecture:
- compute_ground_truth_risk(task, person, cal) — 10 risk rules + noise (see eii-experiment.py lines 157-238)
- MLP architectures: TimePressure (16,8), Dependency (16,8), Staleness (8,), EnergyContext (12,6)
- Pipeline: StandardScaler + MLPClassifier with early_stopping=True
- Export: skl2onnx with opset 15, FloatTensorType, zipmap=False

EII experiment feature names → vectors.json name mapping (CRITICAL — EII used different names):
- EII `age_days` → vectors.json `age_norm`
- EII `days_since_touched` → vectors.json `staleness_norm`
- EII `has_person_dependency` → vectors.json `has_person_dep`
- EII `local_time_pressure_score` → vectors.json `time_pressure_score`
- EII `prev_blocked_probability` → NOT in vectors.json (use entity_reliability + entity_resp_* instead)
- EII `prev_energy_fit_score` → vectors.json `prev_energy_fit`
- EII time_lt15..time_gt60 → NOT in vectors.json task dims (these were EII-synthetic; closest is time_pressure_score)
- EII `dependency_reliability_score` → vectors.json person `reliability_score`
- EII `resp_slow` → vectors.json person `resp_slow`
- EII `resp_unpredictable` → vectors.json person `resp_unknown`

NOTE: The EII experiment used 27-task+23-person+34-calendar=84 feature dims. The production vectors.json also has 27+23+34=84. But the dimension NAMES and semantic ordering differ between EII and vectors.json. The training script must use vectors.json names as the authority — it generates synthetic data matching vectors.json dimension semantics, NOT eii-experiment.py's dimension ordering.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specialist training pipeline and ONNX export</name>
  <files>scripts/train/70_train_specialist_models.py, public/models/specialists/time-pressure-risk.onnx, public/models/specialists/dependency-risk.onnx, public/models/specialists/staleness-risk.onnx, public/models/specialists/energy-context-risk.onnx</files>
  <action>
    Create `scripts/train/70_train_specialist_models.py` — a standalone Python script that:

    1. **Loads vectors.json** to derive canonical dimension names and indices:
       ```python
       import json, os
       VECTORS_PATH = os.path.join(os.path.dirname(__file__), '..', '..', 'src', 'config', 'binder-types', 'gtd-personal', 'vectors.json')
       with open(VECTORS_PATH) as f:
           vschema = json.load(f)['vectorSchema']
       TASK_DIMS = vschema['task']    # 27 names
       PERSON_DIMS = vschema['person']  # 23 names
       CAL_DIMS = vschema['calendar']  # 34 names
       ALL_DIMS = TASK_DIMS + PERSON_DIMS + CAL_DIMS  # 84 names
       ```

    2. **Defines feature slices by dimension name lookup** (never by hardcoded index):
       ```python
       def idx(name): return ALL_DIMS.index(name)
       def idx_range(start_name, end_name): return list(range(ALL_DIMS.index(start_name), ALL_DIMS.index(end_name) + 1))
       ```
       - **TimePressure**: task [has_deadline, days_to_deadline_norm, time_pressure_score] + full calendar range (all 34 dims starting at index 50). MLP: (16, 8).
       - **Dependency**: task [is_waiting_for, has_person_dep, entity_reliability, entity_resp_fast, entity_resp_slow, entity_resp_unknown] + full person range (all 23 dims starting at index 27). MLP: (16, 8).
       - **Staleness**: task [age_norm, staleness_norm, has_deadline, days_to_deadline_norm, prev_staleness_score]. MLP: (8,).
       - **EnergyContext**: task [ctx_home..ctx_anywhere (6), energy_low..energy_high (3), time_pressure_score, prev_energy_fit] + calendar [energy_low, energy_medium, energy_high, time_pressure_score, overrun_risk]. MLP: (12, 6).

    3. **Synthetic data generation** — generate 500 users x 500-2000 tasks each (same scale as eii-experiment.py). For each sample, generate a flat 84-dim vector following vectors.json dimension semantics:
       - Task dims: age_norm exponential(30)/365 capped [0,1], staleness_norm derived from age, has_deadline bernoulli(0.6), etc. — replicate the eii-experiment.py generate_task_vector() logic but mapped to vectors.json dimension names.
       - Person dims: relationship one-hot (9 slots matching vectors.json), mention/recency/confidence continuous, etc.
       - Calendar dims: temporal features, energy one-hot, slack one-hot, etc.
       Note: vectors.json has some dimensions that EII didn't have (enrichment_depth_norm, entity_resp_fast/slow/unknown, etc.) and EII had some that vectors.json doesn't (time_lt15..time_gt60, prev_blocked_probability). Generate the new dims with reasonable distributions. For dims that existed in EII under different names, keep the same distributions.

    4. **Ground truth risk formula** — copy `compute_ground_truth_risk()` from eii-experiment.py verbatim with a comment "# Source: scripts/eii-experiment.py — do not diverge". Adapt index references to use `idx()` name-based lookup instead of hardcoded integers.

    5. **Train 4 specialist models** using `Pipeline([StandardScaler(), MLPClassifier(hidden_layer_sizes=..., max_iter=300, random_state=42, early_stopping=True)])` — same pattern as eii-experiment.py and 61_train_cognitive_models.py.

    6. **Export each model as ONNX** using skl2onnx:
       ```python
       from skl2onnx import convert_sklearn
       from skl2onnx.common.data_types import FloatTensorType
       onnx_model = convert_sklearn(model, initial_types=[('X', FloatTensorType([None, n_features]))],
                                     target_opset=15, options={'zipmap': False})
       ```
       Write to `public/models/specialists/{name}-risk.onnx`. Print size in KB. Assert each is under 20KB.

    7. **Print summary** with per-specialist accuracy, ROC-AUC, feature count, and file size.

    8. **Validate ONNX models** with onnxruntime after export — load each model, run predict on a sample, verify output shape is [1, 2] (2-class probability).

    After writing the script, run it:
    ```bash
    python -u scripts/train/70_train_specialist_models.py
    ```

    Verify the 4 ONNX files are created in `public/models/specialists/` and each is under 20KB.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); ['time-pressure','dependency','staleness','energy-context'].forEach(n => { const p = 'public/models/specialists/'+n+'-risk.onnx'; const s = fs.statSync(p).size; console.log(n+': '+Math.round(s/1024*10)/10+'KB'); if(s > 20480) throw new Error(n+' too large: '+s) }); console.log('All models under 20KB')"</automated>
  </verify>
  <done>4 ONNX models exist at public/models/specialists/{time-pressure,dependency,staleness,energy-context}-risk.onnx, each under 20KB. Training script loads vectors.json for dimension names. Ground truth risk formula matches eii-experiment.py.</done>
</task>

</tasks>

<verification>
- `ls -la public/models/specialists/*.onnx` — 4 files exist
- Each model under 20KB
- `python -c "import onnxruntime as ort; s = ort.InferenceSession('public/models/specialists/staleness-risk.onnx'); print(s.get_inputs()[0].shape)"` — validates model loads
</verification>

<success_criteria>
- 4 specialist ONNX models trained and committed to public/models/specialists/
- Training script derives feature slices from vectors.json (not hardcoded)
- Models use same ground-truth risk formula as eii-experiment.py
- Each model under 20KB
</success_criteria>

<output>
After completion, create `.planning/phases/36-consensus-wired-gate-and-enrichment/36-02-SUMMARY.md`
</output>
